const listCardsBox = document.querySelector('#list_cards');
const cardSelected = document.querySelector('#card_selected');
const currentCardSelectedBox = document.querySelector('#card_selected');
const countWinsBox = document.querySelector('#count_wins');
const resetGameButton = document.querySelector('#reset_game');
const modalStatusBox = document.querySelector('#modal_status');

export {
    listCardsBox,
    cardSelected,
    currentCardSelectedBox,
    countWinsBox,
    resetGameButton,
    modalStatusBox
}